# Hosts the cloud dev environment needs unblocked
#
# Every one of these is CURRENTLY BLOCKED by this sandbox's egress proxy
# (verified 2026-09-08: all nine return 000 / no connection). Listed here so
# the environment's network policy can be updated in one pass.
#
# Nothing secret belongs in this file — hostnames only. The PostHog personal
# API key and the Sketchfab token stay out of the repo entirely.

# PostHog — analytics. Blocking these means the dashboard and the ingest API
# cannot be reached from a session, so any claim about what players did has to
# be taken on trust rather than checked. us.i / eu.i are the ingest hosts the
# browser SDK posts to; app / us / eu are the dashboard and query API.
us.posthog.com
eu.posthog.com
app.posthog.com
us.i.posthog.com
eu.i.posthog.com

# Sketchfab — where the donor car and prop models come from. api is the
# search/download API, media serves the model files themselves.
sketchfab.com
api.sketchfab.com
media.sketchfab.com

# Notion
app.notion.com
